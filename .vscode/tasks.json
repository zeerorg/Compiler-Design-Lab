{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "pytest -v"
        },
        {
            "label": "Coverage Test",
            "type": "shell",
            "command": "coverage run -m pytest -v && coverage report"
        }
    ]
}